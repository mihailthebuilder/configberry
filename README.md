# CloudConfigs

## Notes

Cloudflare SDK: https://github.com/cloudflare/cloudflare-typescript

Cloudflare API: https://developers.cloudflare.com/api/

To figure out the right SDK method, go to the API, and replace the `cURL` dropdown with `TypeScript`.

I need to run a reverse proxy for the requests because of CORS policy on the Cloudflare API.
